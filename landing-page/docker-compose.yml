services:

  frontend:

    build: frontend

    ports:
      - "80:80"

    restart: always

  backend:
    build: backend
    restart: always
    volumes:
      - ./logs:/app/logs
    ports:
      - "8080:8080"
